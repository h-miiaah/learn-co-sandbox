# this creates an infinite loop
# to exit the loop in the terminal hit Ctrl + C or close the terminal window

def looping
  loop do
    puts "Never pass a school bus if it has flashing red lights"
  end
end

looping